from setuptools import setup, find_packages

setup(
    name="Object_Detection",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List dependencies here, e.g. 'tensorflow', 'numpy', etc.
    ],
)
